# Hero image slot

Place the final hero image here as `hero-sphere.webp`, a 16:9 crop, referenced in `src/components/sections/Hero.tsx` and `FRONTEND_SPEC.md`.

No placeholder or generated image is included here on purpose. Until the real file is added, the hero section falls back to a solid background colour.
